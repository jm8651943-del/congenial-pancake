{
  "name": "optimize-primeforge-jobber-bridge",
  "private": true,
  "version": "1.1.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "check": "find api lib public -type f -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  }
}
